#Function code

exports.handler = async (event) => {
    const randomNumber = Math.floor(Math.random() * 100);
    
    const response = {
        statusCode: 200,
        body: JSON.stringify({
            randomNumber: randomNumber
        }),
    }; 
    return response;
};
